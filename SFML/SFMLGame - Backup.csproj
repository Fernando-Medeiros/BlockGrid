<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="SFML.Net" Version="2.6.0" />
  </ItemGroup>

  <Target Name="PreBuild" BeforeTargets="PreBuildEvent">
	  <Exec Command="mkdir $(TargetDir)resources&#xD;&#xA;xcopy /E /I /Y $(SolutionDir)resources $(TargetDir)resources\" />
	  <Exec Command="xcopy /E /I /Y $(SolutionDir)resources $(TargetDir)resources\" />
  </Target>

</Project>
